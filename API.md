<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

- [ok][1]
  - [Parameters][2]
  - [Examples][3]
- [error][4]
  - [Parameters][5]
  - [Examples][6]
- [errors][7]
  - [Parameters][8]
  - [Examples][9]
- [up][10]
  - [Examples][11]
- [Ref][12]
- [ref][13]
  - [Parameters][14]
  - [Examples][15]
- [isRef][16]
  - [Parameters][17]
  - [Examples][18]
- [withRefs][19]
  - [Parameters][20]
  - [Examples][21]
- [test][22]
  - [Parameters][23]
  - [Examples][24]
- [transform][25]
  - [Parameters][26]
  - [Examples][27]
- [pipe][28]
  - [Parameters][29]
  - [Examples][30]
- [oneOf][31]
  - [Parameters][32]
  - [Examples][33]
- [equal][34]
  - [Parameters][35]
  - [Examples][36]
- [notEqual][37]
  - [Parameters][38]
  - [Examples][39]
- [required][40]
  - [Parameters][41]
  - [Examples][42]
- [optional][43]
  - [Parameters][44]
  - [Examples][45]
- [defaultTo][46]
  - [Parameters][47]
  - [Examples][48]
- [any][49]
  - [Examples][50]
- [boolean][51]
  - [Parameters][52]
  - [Examples][53]
- [number][54]
  - [Parameters][55]
  - [Examples][56]
- [string][57]
  - [Parameters][58]
  - [Examples][59]
- [date][60]
  - [Parameters][61]
  - [Examples][62]
- [object][63]
  - [Parameters][64]
  - [Examples][65]
- [array][66]
  - [Parameters][67]
  - [Examples][68]
- [toBoolean][69]
  - [Examples][70]
- [toNumber][71]
  - [Examples][72]
- [parseInt][73]
  - [Parameters][74]
  - [Examples][75]
- [parseFloat][76]
  - [Parameters][77]
  - [Examples][78]
- [toString][79]
  - [Examples][80]
- [toDate][81]
  - [Examples][82]
- [floor][83]
  - [Examples][84]
- [ceil][85]
  - [Examples][86]
- [round][87]
  - [Examples][88]
- [truncate][89]
  - [Examples][90]
- [integer][91]
  - [Parameters][92]
  - [Examples][93]
- [finite][94]
  - [Parameters][95]
  - [Examples][96]
- [min][97]
  - [Parameters][98]
  - [Examples][99]
- [max][100]
  - [Parameters][101]
  - [Examples][102]
- [lessThan][103]
  - [Parameters][104]
  - [Examples][105]
- [moreThan][106]
  - [Parameters][107]
  - [Examples][108]
- [trim][109]
  - [Examples][110]
- [toLower][111]
  - [Examples][112]
- [toUpper][113]
  - [Examples][114]
- [pattern][115]
  - [Parameters][116]
  - [Examples][117]
- [shape][118]
  - [Parameters][119]
  - [Examples][120]
- [values][121]
  - [Parameters][122]
  - [Examples][123]
- [minLength][124]
  - [Parameters][125]
  - [Examples][126]
- [maxLength][127]
  - [Parameters][128]
  - [Examples][129]
- [items][130]
  - [Parameters][131]
  - [Examples][132]
- [tuple][133]
  - [Parameters][134]
  - [Examples][135]

## ok

Creates a valid result.

### Parameters

- `value` **[any][136]** a valid value

### Examples

```javascript
C.ok(42); // => { isOk: true, value: 42 }
```

Returns **[Object][137]** a valid result wrapping the value.

## error

Creates an invalid result with one error.

### Parameters

- `value` **[any][136]** an invalid value
- `message` **[any][136]** an error message
- `path` **[Array][138]** a sequence of keys leading to the invalid value
  (optional, default `[]`)

### Examples

```javascript
C.error(43, "is not the answer"); // => { isOk: false, errors: [ ... ] }
```

Returns **[Object][137]** an invalid result wrapping the error.

## errors

Creates an invalid result with multiple errors.

### Parameters

- `errors` **[Array][138]** a list of error objects

### Examples

```javascript
const result1 = C.error(41, "is lesser than the answer");
const result2 = C.error(43, "is greater than the answer");

C.errors([...result1.errors, ...result2.errors]); // => { isOk: false, errors: [ ... ] }
```

Returns **[Object][137]** an invalid result wrapping the errors.

## up

A constant to use to go up the hierarchy in ref paths.

### Examples

```javascript
const check = C.shape({
  a: C.string(),
  b: C.shape({
    c: C.equal(C.ref([C.up, "a"])),
  }),
});

check({
  a: "asdf",
  b: {
    c: "asdf",
  },
}); // => { isOk: true, ... }
check({
  a: "asdf",
  b: {
    c: "fdsa",
  },
}); // => { isOk: false, ... }
```

## Ref

A reference to a value located in a parent object.

Type: [Object][137]

## ref

Creates a reference to a value located in a parent object.

A reference to a value that does not exist will yield undefined.

The ref references the value before any sibling check functions have been
applied.

You can give a check function if you need to validate or transform the value
before using it.

### Parameters

- `path` **[Array][138]** an array of keys leading to the referenced value
- `check` **[Function][139]** a check function to apply to the referenced value
  (optional, default `ok`)
- `keepErrors` **[boolean][140]** whether or not to keep the errors given by the
  check function (optional, default `false`)

### Examples

```javascript
const check = C.shape({
  password: C.string(),
  passwordConfirmation: C.equal(C.ref(["password"])),
});

check({
  password: "abcd",
  passwordConfirmation: "abcd",
}); // => { isOk: true, ... }
check({
  password: "abcd",
  passwordConfirmation: "dcba",
}); // => { isOk: false, ... }
```

```javascript
const check = C.shape({
  email: C.pipe(C.string(), C.trim()),
  emailConfirmation: C.pipe(
    C.string(),
    C.trim(),
    C.equal(C.ref(["email"], C.pipe(C.string(), C.trim()))),
  ),
});

check({
  email: "   john@doe.com ",
  emailConfirmation: " john@doe.com    ",
}); // => { isOk: true, ... }
```

Returns **[Ref][141]** a ref object

## isRef

Checks whether a given value is a ref object.

### Parameters

- `maybeRef` **[any][136]** a value to check

### Examples

```javascript
C.isRef(C.ref(["password"])); // => true
C.isRef({}); // => false
```

Returns **[boolean][140]** whether the value is a ref object

## withRefs

Creates a check function that uses refs.

If a given ref is not a ref object, it will be given as is to the check
function.

### Parameters

- `refs` **[Array][138]** an array of values to get the references from
- `check` **[Function][139]** a check function that takes the referenced values

### Examples

```javascript
const check = C.shape({
  password: C.string(),
  passwordConfirmation: C.withRefs([C.ref(["password"])], (value, password) =>
    value === password
      ? C.ok(value)
      : C.error(value, "does not match password"),
  ),
});
```

Returns **[Function][139]** a check function.

## test

Creates a check function that fails if a given predicate fails.

All arguments passed to the check function are passed to the predicate function.

### Parameters

- `predicate` **[Function][139]** a function returning a boolean
- `message` **[any][136]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.test(value => value === 42);

check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## transform

Creates a check function that transforms a value into another.

All arguments passed to the check function are passed to the transform function.

### Parameters

- `transform` **[Function][139]** a function transforming a value into another

### Examples

```javascript
const check = C.transform(value => value / 2);

check(42); // => { isOk: true, value 21 }
```

Returns **[Function][139]** a check function.

## pipe

Creates a check function that chains a set of child check functions.

The chain stops at the first failure.

All additional arguments passed after the value to the parent check function are
passed to the child check functions.

### Parameters

- `checks` **...[Function][139]** check functions to chain

### Examples

```javascript
const check = C.pipe(
  C.integer(),
  C.transform(value => value * 2),
  C.max(99),
);

check(25); // => { isOk: true, value: 50 }
check(50); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## oneOf

Creates a check function that returns the result from the first given check
function that succeeds.

If all child check functions fail, the last failing result is returned.

All additional arguments passed after the value to the parent check function are
passed to the child check functions.

### Parameters

- `checks` **...[Function][139]** check functions

### Examples

```javascript
const check = C.oneOf(C.integer(), C.string());

check(42); // => { isOk: true, ... }
check("jerome"); // => { isOk: true, ... }
check(true); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## equal

Creates a check function that fails if a value is not equal to another.

### Parameters

- `value` **([any][136] \| [Ref][141])** a value to check equality against
- `message` **[any][136]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.equal(42);

check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## notEqual

Creates a check function that fails if a value is equal to another.

### Parameters

- `value` **([any][136] \| [Ref][141])** a value to check equality against
- `message` **[any][136]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.notEqual(42);

check(43); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## required

Creates a check function that fails if a value is null or undefined.

### Parameters

- `message` **[any][136]** an error message (optional, default `"is required"`)

### Examples

```javascript
const check = C.required();

check(42); // => { isOk: true, ... }
check(null); // => { isOk: false, ... }
check(undefined); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## optional

Creates a check function that succeeds if a value is null or undefined or if a
given check function succeeds.

### Parameters

- `check` **[Function][139]** a check function

### Examples

```javascript
const check = C.optional(C.number());

check(null); // => { isOk: true, value: null }
check(undefined); // => { isOk: true, value: null }
check(42); // => { isOk: true, value: 42 }
check("jerome"); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## defaultTo

Creates a check function that replaces null or undefined values with a default
value.

### Parameters

- `defaultValue` **([any][136] \| [Ref][141])** a default value

### Examples

```javascript
const check = C.defaultTo(42);

check(3); // => { isOk: true, value: 3 }
check(null); // => { isOk: true, value: 42 }
check(undefined); // => { isOk: true, value: 42 }
```

Returns **[Function][139]** a check function.

## any

Creates a check function that always succeeds.

### Examples

```javascript
const check = C.any();

check(42); // => { isOk: true, value: 42 }
check("forty-two"); // => { isOk: true, value: "forty-two" }
```

Returns **[Function][139]** a check function.

## boolean

Creates a check function that fails when the value is not a boolean.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not a boolean"`)

### Examples

```javascript
const check = C.boolean();

check(true); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## number

Creates a check function that fails when the value is not a number.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not a number"`)

### Examples

```javascript
const check = C.number();

check(42); // => { isOk: true, ... }
check("42"); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## string

Creates a check function that fails when the value is not a string.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not a string"`)

### Examples

```javascript
const check = C.string();

check("forty-two"); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## date

Creates a check function that fails when the value is not a date.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not a date"`)

### Examples

```javascript
const check = C.date();

check(new Date()); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## object

Creates a check function that fails when the value is not an object.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not an object"`)

### Examples

```javascript
const check = C.object();

check({}); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## array

Creates a check function that fails when the value is not an array.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not an array"`)

### Examples

```javascript
const check = C.array();

check([]); // => { isOk: true, ... }
check({}); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## toBoolean

Creates a check function that converts a value of any type into a boolean.

### Examples

```javascript
const check = C.toBoolean();

check(1); // => { isOk: true, value: true }
check(0); // => { isOk: true, value: false }
```

Returns **[Function][139]** a check function.

## toNumber

Creates a check function that converts a value of any type into a number.

### Examples

```javascript
const check = C.toNumber();

check("42"); // => { isOk: true, value: 42 }
check(true); // => { isOk: true, value: 1 }
```

Returns **[Function][139]** a check function.

## parseInt

Creates a check function that tries to parse a string into an integer.

### Parameters

- `radix` **[number][142]** an integer representing the base used to parse the
  number
- `message` **[any][136]** an error message used when the string could not be
  parsed

### Examples

```javascript
const check = C.parseInt();

check("123"); // => { isOk: true, value: 123 }
check("abc"); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## parseFloat

Creates a check function that tries to parse a string into a floating point
number.

### Parameters

- `message` **[any][136]** an error message used when the string could not be
  parsed

### Examples

```javascript
const check = C.parseFloat();

check("3.14"); // => { isOk: true, value: 3.14 }
check("abc"); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## toString

Creates a check function that converts a value of any type into a string.

### Examples

```javascript
const check = C.toString();

check(42); // => { isOk: true, value: "42" }
check(true); // => { isOk: true, value: "true" }
check(null); // => { isOk: true, value: "" }
```

Returns **[Function][139]** a check function.

## toDate

Creates a check function that converts a value of any type into a date.

### Examples

```javascript
const check = C.toDate();

check("2020-09-03"); // => { isOk: true, value: Date(...) }
check(1583749507431); // => { isOk: true, value: Date(...) }
```

Returns **[Function][139]** a check function.

## floor

Creates a check function that rounds a number down to the previous integer.

### Examples

```javascript
const check = C.floor();

check(3.14); // => { isOk: true, value: 3 }
check(3); // => { isOk: true, value: 3 }
```

Returns **[Function][139]** a check function.

## ceil

Creates a check function that rounds a number up to the next integer.

### Examples

```javascript
const check = C.ceil();

check(3.14); // => { isOk: true, value: 4 }
check(3); // => { isOk: true, value: 3 }
```

Returns **[Function][139]** a check function.

## round

Creates a check function that rounds a number to the nearest integer.

### Examples

```javascript
const check = C.round();

check(3.14); // => { isOk: true, value: 3 }
check(3.86); // => { isOk: true, value: 4 }
```

Returns **[Function][139]** a check function.

## truncate

Creates a check function that transforms a number into its integer part.

### Examples

```javascript
const check = C.truncate();

check(3.14); // => { isOk: true, value: 3 }
check(-3.14); // => { isOk: true, value: -3 }
```

Returns **[Function][139]** a check function.

## integer

Creates a check function that fails when the value is not an integer.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not an integer"`)

### Examples

```javascript
const check = C.integer();

check(42); // => { isOk: true, ... }
check(3.14); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## finite

Creates a check function that fails when the value is not a finite number.

### Parameters

- `message` **[any][136]** an error message (optional, default
  `"is not a finite number"`)

### Examples

```javascript
const check = C.finite();

check(42); // => { isOk: true, ... }
check(Infinity); // => { isOk: false, ... }
check(NaN); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## min

Creates a check function that fails if a value is less than a minimum.

### Parameters

- `min` **([number][142] \| [Ref][141])** a value to compare against
- `message` **[any][136]** an error message (optional, default `"is too low"`)

### Examples

```javascript
const check = C.min(42);

check(43); // => { isOk: true, ... }
check(42); // => { isOk: true, ... }
check(41); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## max

Creates a check function that fails if a value is greater than a maximum.

### Parameters

- `max` **([number][142] \| [Ref][141])** a value to compare against
- `message` **[any][136]** an error message (optional, default `"is too high"`)

### Examples

```javascript
const check = C.max(42);

check(41); // => { isOk: true, ... }
check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## lessThan

Creates a check function that fails if a value is greater than or equal to a
maximum.

### Parameters

- `max` **([number][142] \| [Ref][141])** a value to compare against
- `message` **[any][136]** an error message (optional, default `"is too high"`)

### Examples

```javascript
const check = C.lessThan(42);

check(41.99); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## moreThan

Creates a check function that fails if a value is less than or equal to a
minimum.

### Parameters

- `min` **([number][142] \| [Ref][141])** a value to compare against
- `message` **[any][136]** an error message (optional, default `"is too low"`)

### Examples

```javascript
const check = C.moreThan(42);

check(42.01); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## trim

Creates a check function that trims a string.

### Examples

```javascript
const check = C.trim();

check("  jerome   "); // => { isOk: true, value: "jerome" }
```

Returns **[Function][139]** a check function.

## toLower

Creates a check function that converts a string to lower case.

### Examples

```javascript
const check = C.toLower();

check("JEROME"); // => { isOk: true, value: "jerome" }
```

Returns **[Function][139]** a check function.

## toUpper

Creates a check function that converts a string to upper case.

### Examples

```javascript
const check = C.toUpper();

check("jerome"); // => { isOk: true, value: "JEROME" }
```

Returns **[Function][139]** a check function.

## pattern

Creates a check function that fails if a string doesn't match a regular
expression.

### Parameters

- `regexp` **[RegExp][143]** a regular expression to test against
- `message` **[any][136]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.pattern(/[a-z]/);

check("jerome"); // => { isOk: true, ... }
check("42"); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## shape

Creates a check function that will run different check functions on given
properties of an object.

### Parameters

- `checks` **[Object][137]** an object containing check functions

### Examples

```javascript
const check = C.shape({
  name: C.string(),
  age: C.number(),
});

check({
  name: "jerome",
  age: 42,
}); // => { isOk: true, ... }
check({
  name: "jerome",
  age: "forty-two",
}); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## values

Creates a check function that will run the same check function on all of the
values of an object.

### Parameters

- `check` **[Function][139]** a check function

### Examples

```javascript
const check = C.values(C.number());

check({
  one: 42,
  two: 43,
}); // => { isOk: true, ... }
check({
  invalid: "forty-two",
}); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## minLength

Creates a check function that fails if the length of a value is less than a
minimum.

### Parameters

- `min` **([number][142] \| [Ref][141])** a value to compare the length against
- `message` **[any][136]** an error message (optional, default `"is too short"`)

### Examples

```javascript
const check = C.minLength(2);

check([1, 2, 3]); // => { isOk: true, ... }
check([1, 2]); // => { isOk: true, ... }
check([1]); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## maxLength

Creates a check function that fails if the length of a value is greater than a
maximum.

### Parameters

- `max` **([number][142] \| [Ref][141])** a value to compare the length against
- `message` **[any][136]** an error message (optional, default `"is too long"`)

### Examples

```javascript
const check = C.maxLength(3);

check([1, 2]); // => { isOk: true, ... }
check([1, 2, 3]); // => { isOk: true, ... }
check([1, 2, 3, 4]); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## items

Creates a check function that runs the same check function on all of the values
of an array.

### Parameters

- `check` **[Function][139]** a check function

### Examples

```javascript
const check = C.items(C.number());

check([42, 43]); // => { isOk: true, ... }
check(["forty-two"]); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

## tuple

Creates a check that runs different check functions at given positions of an
array.

### Parameters

- `checks` **[Array][138]** an array of check functions
- `lengthMessage` **[any][136]** an error message returned if the value does not
  have the right length

### Examples

```javascript
const check = C.tuple([C.number(), C.string()]);

check([42, "jerome"]); // => { isOk: true, ... }
check(["forty-two", "jerome"]); // => { isOk: false, ... }
check([42]); // => { isOk: false, ... }
```

Returns **[Function][139]** a check function.

[1]: #ok
[2]: #parameters
[3]: #examples
[4]: #error
[5]: #parameters-1
[6]: #examples-1
[7]: #errors
[8]: #parameters-2
[9]: #examples-2
[10]: #up
[11]: #examples-3
[12]: #ref
[13]: #ref-1
[14]: #parameters-3
[15]: #examples-4
[16]: #isref
[17]: #parameters-4
[18]: #examples-5
[19]: #withrefs
[20]: #parameters-5
[21]: #examples-6
[22]: #test
[23]: #parameters-6
[24]: #examples-7
[25]: #transform
[26]: #parameters-7
[27]: #examples-8
[28]: #pipe
[29]: #parameters-8
[30]: #examples-9
[31]: #oneof
[32]: #parameters-9
[33]: #examples-10
[34]: #equal
[35]: #parameters-10
[36]: #examples-11
[37]: #notequal
[38]: #parameters-11
[39]: #examples-12
[40]: #required
[41]: #parameters-12
[42]: #examples-13
[43]: #optional
[44]: #parameters-13
[45]: #examples-14
[46]: #defaultto
[47]: #parameters-14
[48]: #examples-15
[49]: #any
[50]: #examples-16
[51]: #boolean
[52]: #parameters-15
[53]: #examples-17
[54]: #number
[55]: #parameters-16
[56]: #examples-18
[57]: #string
[58]: #parameters-17
[59]: #examples-19
[60]: #date
[61]: #parameters-18
[62]: #examples-20
[63]: #object
[64]: #parameters-19
[65]: #examples-21
[66]: #array
[67]: #parameters-20
[68]: #examples-22
[69]: #toboolean
[70]: #examples-23
[71]: #tonumber
[72]: #examples-24
[73]: #parseint
[74]: #parameters-21
[75]: #examples-25
[76]: #parsefloat
[77]: #parameters-22
[78]: #examples-26
[79]: #tostring
[80]: #examples-27
[81]: #todate
[82]: #examples-28
[83]: #floor
[84]: #examples-29
[85]: #ceil
[86]: #examples-30
[87]: #round
[88]: #examples-31
[89]: #truncate
[90]: #examples-32
[91]: #integer
[92]: #parameters-23
[93]: #examples-33
[94]: #finite
[95]: #parameters-24
[96]: #examples-34
[97]: #min
[98]: #parameters-25
[99]: #examples-35
[100]: #max
[101]: #parameters-26
[102]: #examples-36
[103]: #lessthan
[104]: #parameters-27
[105]: #examples-37
[106]: #morethan
[107]: #parameters-28
[108]: #examples-38
[109]: #trim
[110]: #examples-39
[111]: #tolower
[112]: #examples-40
[113]: #toupper
[114]: #examples-41
[115]: #pattern
[116]: #parameters-29
[117]: #examples-42
[118]: #shape
[119]: #parameters-30
[120]: #examples-43
[121]: #values
[122]: #parameters-31
[123]: #examples-44
[124]: #minlength
[125]: #parameters-32
[126]: #examples-45
[127]: #maxlength
[128]: #parameters-33
[129]: #examples-46
[130]: #items
[131]: #parameters-34
[132]: #examples-47
[133]: #tuple
[134]: #parameters-35
[135]: #examples-48
[136]: #any
[137]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object
[138]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array
[139]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/function
[140]: #boolean
[141]: #ref
[142]: #number
[143]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/RegExp
