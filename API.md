<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

- [ok][1]
  - [Parameters][2]
  - [Examples][3]
- [error][4]
  - [Parameters][5]
  - [Examples][6]
- [errors][7]
  - [Parameters][8]
  - [Examples][9]
- [Ref][10]
- [test][11]
  - [Parameters][12]
  - [Examples][13]
- [transform][14]
  - [Parameters][15]
  - [Examples][16]
- [pipe][17]
  - [Parameters][18]
  - [Examples][19]
- [oneOf][20]
  - [Parameters][21]
  - [Examples][22]
- [equal][23]
  - [Parameters][24]
  - [Examples][25]
- [required][26]
  - [Parameters][27]
  - [Examples][28]
- [optional][29]
  - [Parameters][30]
  - [Examples][31]
- [any][32]
  - [Examples][33]
- [boolean][34]
  - [Parameters][35]
  - [Examples][36]
- [number][37]
  - [Parameters][38]
  - [Examples][39]
- [string][40]
  - [Parameters][41]
  - [Examples][42]
- [date][43]
  - [Parameters][44]
  - [Examples][45]
- [object][46]
  - [Parameters][47]
  - [Examples][48]
- [array][49]
  - [Parameters][50]
  - [Examples][51]
- [toBoolean][52]
  - [Examples][53]
- [toNumber][54]
  - [Examples][55]
- [parseInt][56]
  - [Parameters][57]
  - [Examples][58]
- [parseFloat][59]
  - [Parameters][60]
  - [Examples][61]
- [toString][62]
  - [Examples][63]
- [toDate][64]
  - [Examples][65]
- [floor][66]
  - [Examples][67]
- [ceil][68]
  - [Examples][69]
- [round][70]
  - [Examples][71]
- [truncate][72]
  - [Examples][73]
- [integer][74]
  - [Parameters][75]
  - [Examples][76]
- [finite][77]
  - [Parameters][78]
  - [Examples][79]
- [trim][80]
  - [Examples][81]
- [toLower][82]
  - [Examples][83]
- [toUpper][84]
  - [Examples][85]
- [pattern][86]
  - [Parameters][87]
  - [Examples][88]
- [shape][89]
  - [Parameters][90]
  - [Examples][91]
- [values][92]
  - [Parameters][93]
  - [Examples][94]
- [items][95]
  - [Parameters][96]
  - [Examples][97]
- [tuple][98]
  - [Parameters][99]
  - [Examples][100]

## ok

Creates a valid result.

### Parameters

- `value` **[any][101]** a valid value

### Examples

```javascript
C.ok(42); // => { isOk: true, value: 42 }
```

Returns **[Object][102]** a valid result wrapping the value.

## error

Creates an invalid result with one error.

### Parameters

- `value` **[any][101]** an invalid value
- `message` **[any][101]** an error message
- `path` **[Array][103]** a sequence of keys leading to the invalid value
  (optional, default `[]`)

### Examples

```javascript
C.error(43, "is not the answer"); // => { isOk: false, errors: [ ... ] }
```

Returns **[Object][102]** an invalid result wrapping the error.

## errors

Creates an invalid result with multiple errors.

### Parameters

- `errors` **[Array][103]** a list of error objects

### Examples

```javascript
const result1 = C.error(41, "is lesser than the answer");
const result2 = C.error(43, "is greater than the answer");

C.errors([...result1.errors, ...result2.errors]); // => { isOk: false, errors: [ ... ] }
```

Returns **[Object][102]** an invalid result wrapping the errors.

## Ref

A reference to a value present at a particular path in the parent tree.

Type: [Object][102]

## test

Creates a check function that fails if a given predicate fails.

All arguments passed to the check function are passed to the predicate function.

### Parameters

- `predicate` **[Function][104]** a function returning a boolean
- `message` **[any][101]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.test(value => value === 42);

check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## transform

Creates a check function that transforms a value into another.

All arguments passed to the check function are passed to the transform function.

### Parameters

- `transform` **[Function][104]** a function transforming a value into another

### Examples

```javascript
const check = C.transform(value => value / 2);

check(42); // => { isOk: true, value 21 }
```

Returns **[Function][104]** a check function.

## pipe

Creates a check function that chains a set of child check functions.

The chain stops at the first failure.

All additional arguments passed after the value to the parent check function are
passed to the child check functions.

### Parameters

- `checks` **...[Function][104]** check functions to chain

### Examples

```javascript
const check = C.pipe(
  C.integer(),
  C.transform(value => value * 2),
  C.max(99),
);

check(25); // => { isOk: true, value: 50 }
check(50); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## oneOf

Creates a check function that returns the result from the first given check
function that succeeds.

If all child check functions fail, the last failing result is returned.

All additional arguments passed after the value to the parent check function are
passed to the child check functions.

### Parameters

- `checks` **...[Function][104]** check functions

### Examples

```javascript
const check = C.oneOf(C.integer(), C.string());

check(42); // => { isOk: true, ... }
check("jerome"); // => { isOk: true, ... }
check(true); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## equal

Creates a check that fails if a value is not equal to another.

### Parameters

- `value` **([any][101] \| [Ref][105])** a value to check equality against
- `message` **[any][101]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.equal(42);

check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## required

Creates a check function that fails if a value is null or undefined.

### Parameters

- `message` **[any][101]** an error message (optional, default `"is required"`)

### Examples

```javascript
const check = C.required();

check(42); // => { isOk: true, ... }
check(null); // => { isOk: false, ... }
check(undefined); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## optional

Creates a check function that succeeds if a value is null or undefined or if a
given check function succeeds.

### Parameters

- `check` **[Function][104]** a check function

### Examples

```javascript
const check = C.optional(C.number());

check(null); // => { isOk: true, value: null }
check(undefined); // => { isOk: true, value: null }
check(42); // => { isOk: true, value: 42 }
check("jerome"); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## any

Creates a check function that always succeeds.

### Examples

```javascript
const check = C.any();

check(42); // => { isOk: true, value: 42 }
check("forty-two"); // => { isOk: true, value: "forty-two" }
```

Returns **[Function][104]** a check function.

## boolean

Creates a check function that fails when the value is not a boolean.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not a boolean"`)

### Examples

```javascript
const check = C.boolean();

check(true); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## number

Creates a check function that fails when the value is not a number.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not a number"`)

### Examples

```javascript
const check = C.number();

check(42); // => { isOk: true, ... }
check("42"); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## string

Creates a check function that fails when the value is not a string.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not a string"`)

### Examples

```javascript
const check = C.string();

check("forty-two"); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## date

Creates a check function that fails when the value is not a date.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not a date"`)

### Examples

```javascript
const check = C.date();

check(new Date()); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## object

Creates a check function that fails when the value is not an object.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not an object"`)

### Examples

```javascript
const check = C.object();

check({}); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## array

Creates a check function that fails when the value is not an array.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not an array"`)

### Examples

```javascript
const check = C.array();

check([]); // => { isOk: true, ... }
check({}); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## toBoolean

Creates a check function that converts a value of any type into a boolean.

### Examples

```javascript
const check = C.toBoolean();

check(1); // => { isOk: true, value: true }
check(0); // => { isOk: true, value: false }
```

Returns **[Function][104]** a check function.

## toNumber

Creates a check function that converts a value of any type into a number.

### Examples

```javascript
const check = C.toNumber();

check("42"); // => { isOk: true, value: 42 }
check(true); // => { isOk: true, value: 1 }
```

Returns **[Function][104]** a check function.

## parseInt

Creates a check function that tries to parse a string into an integer.

### Parameters

- `radix` **[number][106]** an integer representing the base used to parse the
  number
- `message` **[any][101]** an error message used when the string could not be
  parsed

### Examples

```javascript
const check = C.parseInt();

check("123"); // => { isOk: true, value: 123 }
check("abc"); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## parseFloat

Creates a check function that tries to parse a string into a floating point
number.

### Parameters

- `message` **[any][101]** an error message used when the string could not be
  parsed

### Examples

```javascript
const check = C.parseFloat();

check("3.14"); // => { isOk: true, value: 3.14 }
check("abc"); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## toString

Creates a check function that converts a value of any type into a string.

### Examples

```javascript
const check = C.toString();

check(42); // => { isOk: true, value: "42" }
check(true); // => { isOk: true, value: "true" }
check(null); // => { isOk: true, value: "" }
```

Returns **[Function][104]** a check function.

## toDate

Creates a check function that converts a value of any type into a date.

### Examples

```javascript
const check = C.toDate();

check("2020-09-03"); // => { isOk: true, value: Date(...) }
check(1583749507431); // => { isOk: true, value: Date(...) }
```

Returns **[Function][104]** a check function.

## floor

Creates a check function that rounds a number down to the previous integer.

### Examples

```javascript
const check = C.floor();

check(3.14); // => { isOk: true, value: 3 }
check(3); // => { isOk: true, value: 3 }
```

Returns **[Function][104]** a check function.

## ceil

Creates a check function that rounds a number up to the next integer.

### Examples

```javascript
const check = C.ceil();

check(3.14); // => { isOk: true, value: 4 }
check(3); // => { isOk: true, value: 3 }
```

Returns **[Function][104]** a check function.

## round

Creates a check function that rounds a number to the nearest integer.

### Examples

```javascript
const check = C.round();

check(3.14); // => { isOk: true, value: 3 }
check(3.86); // => { isOk: true, value: 4 }
```

Returns **[Function][104]** a check function.

## truncate

Creates a check function that transforms a number into its integer part.

### Examples

```javascript
const check = C.truncate();

check(3.14); // => { isOk: true, value: 3 }
check(-3.14); // => { isOk: true, value: -3 }
```

Returns **[Function][104]** a check function.

## integer

Creates a check function that fails when the value is not an integer.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not an integer"`)

### Examples

```javascript
const check = C.integer();

check(42); // => { isOk: true, ... }
check(3.14); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## finite

Creates a check function that fails when the value is not a finite number.

### Parameters

- `message` **[any][101]** an error message (optional, default
  `"is not a finite number"`)

### Examples

```javascript
const check = C.finite();

check(42); // => { isOk: true, ... }
check(Infinity); // => { isOk: false, ... }
check(NaN); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## trim

Creates a check function that trims a string.

### Examples

```javascript
const check = C.trim();

check("  jerome   "); // => { isOk: true, value: "jerome" }
```

Returns **[Function][104]** a check function.

## toLower

Creates a check function that converts a string to lower case.

### Examples

```javascript
const check = C.toLower();

check("JEROME"); // => { isOk: true, value: "jerome" }
```

Returns **[Function][104]** a check function.

## toUpper

Creates a check function that converts a string to upper case.

### Examples

```javascript
const check = C.toUpper();

check("jerome"); // => { isOk: true, value: "JEROME" }
```

Returns **[Function][104]** a check function.

## pattern

Creates a check function that fails if a string doesn't match a regular
expression.

### Parameters

- `regexp` **[RegExp][107]** a regular expression to test against
- `message` **[any][101]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.pattern(/[a-z]/);

check("jerome"); // => { isOk: true, ... }
check("42"); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## shape

Creates a check function that will run different check functions on given
properties of an object.

### Parameters

- `checks` **[Object][102]** an object containing check functions

### Examples

```javascript
const check = C.shape({
  name: C.string(),
  age: C.number(),
});

check({
  name: "jerome",
  age: 42,
}); // => { isOk: true, ... }
check({
  name: "jerome",
  age: "forty-two",
}); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## values

Creates a check function that will run the same check function on all of the
values of an object.

### Parameters

- `check` **[Function][104]** a check function

### Examples

```javascript
const check = C.values(C.number());

check({
  one: 42,
  two: 43,
}); // => { isOk: true, ... }
check({
  invalid: "forty-two",
}); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## items

Creates a check function that runs the same check function on all of the values
of an array.

### Parameters

- `check` **[Function][104]** a check function

### Examples

```javascript
const check = C.items(C.number());

check([42, 43]); // => { isOk: true, ... }
check(["forty-two"]); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

## tuple

Creates a check that runs different check functions at given positions of an
array.

### Parameters

- `checks` **[Array][103]** an array of check functions
- `lengthMessage` **[any][101]** an error message returned if the value does not
  have the right length

### Examples

```javascript
const check = C.tuple([C.number(), C.string()]);

check([42, "jerome"]); // => { isOk: true, ... }
check(["forty-two", "jerome"]); // => { isOk: false, ... }
check([42]); // => { isOk: false, ... }
```

Returns **[Function][104]** a check function.

[1]: #ok
[2]: #parameters
[3]: #examples
[4]: #error
[5]: #parameters-1
[6]: #examples-1
[7]: #errors
[8]: #parameters-2
[9]: #examples-2
[10]: #ref
[11]: #test
[12]: #parameters-3
[13]: #examples-3
[14]: #transform
[15]: #parameters-4
[16]: #examples-4
[17]: #pipe
[18]: #parameters-5
[19]: #examples-5
[20]: #oneof
[21]: #parameters-6
[22]: #examples-6
[23]: #equal
[24]: #parameters-7
[25]: #examples-7
[26]: #required
[27]: #parameters-8
[28]: #examples-8
[29]: #optional
[30]: #parameters-9
[31]: #examples-9
[32]: #any
[33]: #examples-10
[34]: #boolean
[35]: #parameters-10
[36]: #examples-11
[37]: #number
[38]: #parameters-11
[39]: #examples-12
[40]: #string
[41]: #parameters-12
[42]: #examples-13
[43]: #date
[44]: #parameters-13
[45]: #examples-14
[46]: #object
[47]: #parameters-14
[48]: #examples-15
[49]: #array
[50]: #parameters-15
[51]: #examples-16
[52]: #toboolean
[53]: #examples-17
[54]: #tonumber
[55]: #examples-18
[56]: #parseint
[57]: #parameters-16
[58]: #examples-19
[59]: #parsefloat
[60]: #parameters-17
[61]: #examples-20
[62]: #tostring
[63]: #examples-21
[64]: #todate
[65]: #examples-22
[66]: #floor
[67]: #examples-23
[68]: #ceil
[69]: #examples-24
[70]: #round
[71]: #examples-25
[72]: #truncate
[73]: #examples-26
[74]: #integer
[75]: #parameters-18
[76]: #examples-27
[77]: #finite
[78]: #parameters-19
[79]: #examples-28
[80]: #trim
[81]: #examples-29
[82]: #tolower
[83]: #examples-30
[84]: #toupper
[85]: #examples-31
[86]: #pattern
[87]: #parameters-20
[88]: #examples-32
[89]: #shape
[90]: #parameters-21
[91]: #examples-33
[92]: #values
[93]: #parameters-22
[94]: #examples-34
[95]: #items
[96]: #parameters-23
[97]: #examples-35
[98]: #tuple
[99]: #parameters-24
[100]: #examples-36
[101]: #any
[102]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object
[103]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array
[104]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/function
[105]: #ref
[106]: #number
[107]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/RegExp
