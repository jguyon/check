<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

- [ok][1]
  - [Parameters][2]
  - [Examples][3]
- [error][4]
  - [Parameters][5]
  - [Examples][6]
- [errors][7]
  - [Parameters][8]
  - [Examples][9]
- [Ref][10]
- [test][11]
  - [Parameters][12]
  - [Examples][13]
- [transform][14]
  - [Parameters][15]
  - [Examples][16]
- [pipe][17]
  - [Parameters][18]
  - [Examples][19]
- [oneOf][20]
  - [Parameters][21]
  - [Examples][22]
- [equal][23]
  - [Parameters][24]
  - [Examples][25]
- [notEqual][26]
  - [Parameters][27]
  - [Examples][28]
- [required][29]
  - [Parameters][30]
  - [Examples][31]
- [optional][32]
  - [Parameters][33]
  - [Examples][34]
- [any][35]
  - [Examples][36]
- [boolean][37]
  - [Parameters][38]
  - [Examples][39]
- [number][40]
  - [Parameters][41]
  - [Examples][42]
- [string][43]
  - [Parameters][44]
  - [Examples][45]
- [date][46]
  - [Parameters][47]
  - [Examples][48]
- [object][49]
  - [Parameters][50]
  - [Examples][51]
- [array][52]
  - [Parameters][53]
  - [Examples][54]
- [toBoolean][55]
  - [Examples][56]
- [toNumber][57]
  - [Examples][58]
- [parseInt][59]
  - [Parameters][60]
  - [Examples][61]
- [parseFloat][62]
  - [Parameters][63]
  - [Examples][64]
- [toString][65]
  - [Examples][66]
- [toDate][67]
  - [Examples][68]
- [floor][69]
  - [Examples][70]
- [ceil][71]
  - [Examples][72]
- [round][73]
  - [Examples][74]
- [truncate][75]
  - [Examples][76]
- [integer][77]
  - [Parameters][78]
  - [Examples][79]
- [finite][80]
  - [Parameters][81]
  - [Examples][82]
- [min][83]
  - [Parameters][84]
  - [Examples][85]
- [max][86]
  - [Parameters][87]
  - [Examples][88]
- [lessThan][89]
  - [Parameters][90]
  - [Examples][91]
- [moreThan][92]
  - [Parameters][93]
  - [Examples][94]
- [trim][95]
  - [Examples][96]
- [toLower][97]
  - [Examples][98]
- [toUpper][99]
  - [Examples][100]
- [pattern][101]
  - [Parameters][102]
  - [Examples][103]
- [shape][104]
  - [Parameters][105]
  - [Examples][106]
- [values][107]
  - [Parameters][108]
  - [Examples][109]
- [minLength][110]
  - [Parameters][111]
  - [Examples][112]
- [items][113]
  - [Parameters][114]
  - [Examples][115]
- [tuple][116]
  - [Parameters][117]
  - [Examples][118]

## ok

Creates a valid result.

### Parameters

- `value` **[any][119]** a valid value

### Examples

```javascript
C.ok(42); // => { isOk: true, value: 42 }
```

Returns **[Object][120]** a valid result wrapping the value.

## error

Creates an invalid result with one error.

### Parameters

- `value` **[any][119]** an invalid value
- `message` **[any][119]** an error message
- `path` **[Array][121]** a sequence of keys leading to the invalid value
  (optional, default `[]`)

### Examples

```javascript
C.error(43, "is not the answer"); // => { isOk: false, errors: [ ... ] }
```

Returns **[Object][120]** an invalid result wrapping the error.

## errors

Creates an invalid result with multiple errors.

### Parameters

- `errors` **[Array][121]** a list of error objects

### Examples

```javascript
const result1 = C.error(41, "is lesser than the answer");
const result2 = C.error(43, "is greater than the answer");

C.errors([...result1.errors, ...result2.errors]); // => { isOk: false, errors: [ ... ] }
```

Returns **[Object][120]** an invalid result wrapping the errors.

## Ref

A reference to a value present at a particular path in the parent tree.

Type: [Object][120]

## test

Creates a check function that fails if a given predicate fails.

All arguments passed to the check function are passed to the predicate function.

### Parameters

- `predicate` **[Function][122]** a function returning a boolean
- `message` **[any][119]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.test(value => value === 42);

check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## transform

Creates a check function that transforms a value into another.

All arguments passed to the check function are passed to the transform function.

### Parameters

- `transform` **[Function][122]** a function transforming a value into another

### Examples

```javascript
const check = C.transform(value => value / 2);

check(42); // => { isOk: true, value 21 }
```

Returns **[Function][122]** a check function.

## pipe

Creates a check function that chains a set of child check functions.

The chain stops at the first failure.

All additional arguments passed after the value to the parent check function are
passed to the child check functions.

### Parameters

- `checks` **...[Function][122]** check functions to chain

### Examples

```javascript
const check = C.pipe(
  C.integer(),
  C.transform(value => value * 2),
  C.max(99),
);

check(25); // => { isOk: true, value: 50 }
check(50); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## oneOf

Creates a check function that returns the result from the first given check
function that succeeds.

If all child check functions fail, the last failing result is returned.

All additional arguments passed after the value to the parent check function are
passed to the child check functions.

### Parameters

- `checks` **...[Function][122]** check functions

### Examples

```javascript
const check = C.oneOf(C.integer(), C.string());

check(42); // => { isOk: true, ... }
check("jerome"); // => { isOk: true, ... }
check(true); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## equal

Creates a check function that fails if a value is not equal to another.

### Parameters

- `value` **([any][119] \| [Ref][123])** a value to check equality against
- `message` **[any][119]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.equal(42);

check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## notEqual

Creates a check function that fails if a value is equal to another.

### Parameters

- `value` **([any][119] \| [Ref][123])** a value to check equality against
- `message` **[any][119]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.notEqual(42);

check(43); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## required

Creates a check function that fails if a value is null or undefined.

### Parameters

- `message` **[any][119]** an error message (optional, default `"is required"`)

### Examples

```javascript
const check = C.required();

check(42); // => { isOk: true, ... }
check(null); // => { isOk: false, ... }
check(undefined); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## optional

Creates a check function that succeeds if a value is null or undefined or if a
given check function succeeds.

### Parameters

- `check` **[Function][122]** a check function

### Examples

```javascript
const check = C.optional(C.number());

check(null); // => { isOk: true, value: null }
check(undefined); // => { isOk: true, value: null }
check(42); // => { isOk: true, value: 42 }
check("jerome"); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## any

Creates a check function that always succeeds.

### Examples

```javascript
const check = C.any();

check(42); // => { isOk: true, value: 42 }
check("forty-two"); // => { isOk: true, value: "forty-two" }
```

Returns **[Function][122]** a check function.

## boolean

Creates a check function that fails when the value is not a boolean.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not a boolean"`)

### Examples

```javascript
const check = C.boolean();

check(true); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## number

Creates a check function that fails when the value is not a number.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not a number"`)

### Examples

```javascript
const check = C.number();

check(42); // => { isOk: true, ... }
check("42"); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## string

Creates a check function that fails when the value is not a string.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not a string"`)

### Examples

```javascript
const check = C.string();

check("forty-two"); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## date

Creates a check function that fails when the value is not a date.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not a date"`)

### Examples

```javascript
const check = C.date();

check(new Date()); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## object

Creates a check function that fails when the value is not an object.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not an object"`)

### Examples

```javascript
const check = C.object();

check({}); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## array

Creates a check function that fails when the value is not an array.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not an array"`)

### Examples

```javascript
const check = C.array();

check([]); // => { isOk: true, ... }
check({}); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## toBoolean

Creates a check function that converts a value of any type into a boolean.

### Examples

```javascript
const check = C.toBoolean();

check(1); // => { isOk: true, value: true }
check(0); // => { isOk: true, value: false }
```

Returns **[Function][122]** a check function.

## toNumber

Creates a check function that converts a value of any type into a number.

### Examples

```javascript
const check = C.toNumber();

check("42"); // => { isOk: true, value: 42 }
check(true); // => { isOk: true, value: 1 }
```

Returns **[Function][122]** a check function.

## parseInt

Creates a check function that tries to parse a string into an integer.

### Parameters

- `radix` **[number][124]** an integer representing the base used to parse the
  number
- `message` **[any][119]** an error message used when the string could not be
  parsed

### Examples

```javascript
const check = C.parseInt();

check("123"); // => { isOk: true, value: 123 }
check("abc"); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## parseFloat

Creates a check function that tries to parse a string into a floating point
number.

### Parameters

- `message` **[any][119]** an error message used when the string could not be
  parsed

### Examples

```javascript
const check = C.parseFloat();

check("3.14"); // => { isOk: true, value: 3.14 }
check("abc"); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## toString

Creates a check function that converts a value of any type into a string.

### Examples

```javascript
const check = C.toString();

check(42); // => { isOk: true, value: "42" }
check(true); // => { isOk: true, value: "true" }
check(null); // => { isOk: true, value: "" }
```

Returns **[Function][122]** a check function.

## toDate

Creates a check function that converts a value of any type into a date.

### Examples

```javascript
const check = C.toDate();

check("2020-09-03"); // => { isOk: true, value: Date(...) }
check(1583749507431); // => { isOk: true, value: Date(...) }
```

Returns **[Function][122]** a check function.

## floor

Creates a check function that rounds a number down to the previous integer.

### Examples

```javascript
const check = C.floor();

check(3.14); // => { isOk: true, value: 3 }
check(3); // => { isOk: true, value: 3 }
```

Returns **[Function][122]** a check function.

## ceil

Creates a check function that rounds a number up to the next integer.

### Examples

```javascript
const check = C.ceil();

check(3.14); // => { isOk: true, value: 4 }
check(3); // => { isOk: true, value: 3 }
```

Returns **[Function][122]** a check function.

## round

Creates a check function that rounds a number to the nearest integer.

### Examples

```javascript
const check = C.round();

check(3.14); // => { isOk: true, value: 3 }
check(3.86); // => { isOk: true, value: 4 }
```

Returns **[Function][122]** a check function.

## truncate

Creates a check function that transforms a number into its integer part.

### Examples

```javascript
const check = C.truncate();

check(3.14); // => { isOk: true, value: 3 }
check(-3.14); // => { isOk: true, value: -3 }
```

Returns **[Function][122]** a check function.

## integer

Creates a check function that fails when the value is not an integer.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not an integer"`)

### Examples

```javascript
const check = C.integer();

check(42); // => { isOk: true, ... }
check(3.14); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## finite

Creates a check function that fails when the value is not a finite number.

### Parameters

- `message` **[any][119]** an error message (optional, default
  `"is not a finite number"`)

### Examples

```javascript
const check = C.finite();

check(42); // => { isOk: true, ... }
check(Infinity); // => { isOk: false, ... }
check(NaN); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## min

Creates a check function that fails if a value is less than a minimum.

### Parameters

- `min` **([number][124] \| [Ref][123])** a value to compare
- `message` **[any][119]** an error message (optional, default `"is too low"`)

### Examples

```javascript
const check = C.min(42);

check(43); // => { isOk: true, ... }
check(42); // => { isOk: true, ... }
check(41); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## max

Creates a check function that fails if a value is greater than a maximum.

### Parameters

- `max` **([number][124] \| [Ref][123])** a value to compare
- `message` **[any][119]** an error message (optional, default `"is too high"`)

### Examples

```javascript
const check = C.max(42);

check(41); // => { isOk: true, ... }
check(42); // => { isOk: true, ... }
check(43); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## lessThan

Creates a check function that fails if a value is greater than or equal to a
maximum.

### Parameters

- `max` **([number][124] \| [Ref][123])** a value to compare
- `message` **[any][119]** an error message (optional, default `"is too high"`)

### Examples

```javascript
const check = C.lessThan(42);

check(41.99); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## moreThan

Creates a check function that fails if a value is less than or equal to a
minimum.

### Parameters

- `min` **([number][124] \| [Ref][123])** a value to compare
- `message` **[any][119]** an error message (optional, default `"is too low"`)

### Examples

```javascript
const check = C.moreThan(42);

check(42.01); // => { isOk: true, ... }
check(42); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## trim

Creates a check function that trims a string.

### Examples

```javascript
const check = C.trim();

check("  jerome   "); // => { isOk: true, value: "jerome" }
```

Returns **[Function][122]** a check function.

## toLower

Creates a check function that converts a string to lower case.

### Examples

```javascript
const check = C.toLower();

check("JEROME"); // => { isOk: true, value: "jerome" }
```

Returns **[Function][122]** a check function.

## toUpper

Creates a check function that converts a string to upper case.

### Examples

```javascript
const check = C.toUpper();

check("jerome"); // => { isOk: true, value: "JEROME" }
```

Returns **[Function][122]** a check function.

## pattern

Creates a check function that fails if a string doesn't match a regular
expression.

### Parameters

- `regexp` **[RegExp][125]** a regular expression to test against
- `message` **[any][119]** an error message (optional, default `"is invalid"`)

### Examples

```javascript
const check = C.pattern(/[a-z]/);

check("jerome"); // => { isOk: true, ... }
check("42"); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## shape

Creates a check function that will run different check functions on given
properties of an object.

### Parameters

- `checks` **[Object][120]** an object containing check functions

### Examples

```javascript
const check = C.shape({
  name: C.string(),
  age: C.number(),
});

check({
  name: "jerome",
  age: 42,
}); // => { isOk: true, ... }
check({
  name: "jerome",
  age: "forty-two",
}); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## values

Creates a check function that will run the same check function on all of the
values of an object.

### Parameters

- `check` **[Function][122]** a check function

### Examples

```javascript
const check = C.values(C.number());

check({
  one: 42,
  two: 43,
}); // => { isOk: true, ... }
check({
  invalid: "forty-two",
}); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## minLength

Creates a check function that fails if the length of a value is less than a
minimum.

### Parameters

- `min` **([number][124] \| [Ref][123])** a value to compare the length against
- `message` **[any][119]** an error message (optional, default `"is too short"`)

### Examples

```javascript
const check = C.minLength(2);

check([1, 2, 3]); // => { isOk: true, ... }
check([1, 2]); // => { isOk: true, ... }
check([1]); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## items

Creates a check function that runs the same check function on all of the values
of an array.

### Parameters

- `check` **[Function][122]** a check function

### Examples

```javascript
const check = C.items(C.number());

check([42, 43]); // => { isOk: true, ... }
check(["forty-two"]); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

## tuple

Creates a check that runs different check functions at given positions of an
array.

### Parameters

- `checks` **[Array][121]** an array of check functions
- `lengthMessage` **[any][119]** an error message returned if the value does not
  have the right length

### Examples

```javascript
const check = C.tuple([C.number(), C.string()]);

check([42, "jerome"]); // => { isOk: true, ... }
check(["forty-two", "jerome"]); // => { isOk: false, ... }
check([42]); // => { isOk: false, ... }
```

Returns **[Function][122]** a check function.

[1]: #ok
[2]: #parameters
[3]: #examples
[4]: #error
[5]: #parameters-1
[6]: #examples-1
[7]: #errors
[8]: #parameters-2
[9]: #examples-2
[10]: #ref
[11]: #test
[12]: #parameters-3
[13]: #examples-3
[14]: #transform
[15]: #parameters-4
[16]: #examples-4
[17]: #pipe
[18]: #parameters-5
[19]: #examples-5
[20]: #oneof
[21]: #parameters-6
[22]: #examples-6
[23]: #equal
[24]: #parameters-7
[25]: #examples-7
[26]: #notequal
[27]: #parameters-8
[28]: #examples-8
[29]: #required
[30]: #parameters-9
[31]: #examples-9
[32]: #optional
[33]: #parameters-10
[34]: #examples-10
[35]: #any
[36]: #examples-11
[37]: #boolean
[38]: #parameters-11
[39]: #examples-12
[40]: #number
[41]: #parameters-12
[42]: #examples-13
[43]: #string
[44]: #parameters-13
[45]: #examples-14
[46]: #date
[47]: #parameters-14
[48]: #examples-15
[49]: #object
[50]: #parameters-15
[51]: #examples-16
[52]: #array
[53]: #parameters-16
[54]: #examples-17
[55]: #toboolean
[56]: #examples-18
[57]: #tonumber
[58]: #examples-19
[59]: #parseint
[60]: #parameters-17
[61]: #examples-20
[62]: #parsefloat
[63]: #parameters-18
[64]: #examples-21
[65]: #tostring
[66]: #examples-22
[67]: #todate
[68]: #examples-23
[69]: #floor
[70]: #examples-24
[71]: #ceil
[72]: #examples-25
[73]: #round
[74]: #examples-26
[75]: #truncate
[76]: #examples-27
[77]: #integer
[78]: #parameters-19
[79]: #examples-28
[80]: #finite
[81]: #parameters-20
[82]: #examples-29
[83]: #min
[84]: #parameters-21
[85]: #examples-30
[86]: #max
[87]: #parameters-22
[88]: #examples-31
[89]: #lessthan
[90]: #parameters-23
[91]: #examples-32
[92]: #morethan
[93]: #parameters-24
[94]: #examples-33
[95]: #trim
[96]: #examples-34
[97]: #tolower
[98]: #examples-35
[99]: #toupper
[100]: #examples-36
[101]: #pattern
[102]: #parameters-25
[103]: #examples-37
[104]: #shape
[105]: #parameters-26
[106]: #examples-38
[107]: #values
[108]: #parameters-27
[109]: #examples-39
[110]: #minlength
[111]: #parameters-28
[112]: #examples-40
[113]: #items
[114]: #parameters-29
[115]: #examples-41
[116]: #tuple
[117]: #parameters-30
[118]: #examples-42
[119]: #any
[120]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object
[121]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array
[122]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Statements/function
[123]: #ref
[124]: #number
[125]:
  https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/RegExp
